import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { QuotehistoryPage } from './quotehistory';

@NgModule({
  declarations: [
    QuotehistoryPage,
  ],
  imports: [
    IonicPageModule.forChild(QuotehistoryPage),
  ],
})
export class QuotehistoryPageModule {}
